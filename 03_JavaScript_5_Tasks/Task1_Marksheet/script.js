const subjects=["HTML","CSS","JavaScript","Python","MySQL"];const box=document.getElementById("inputs");subjects.forEach((s,i)=>box.insertAdjacentHTML("beforeend",`<label>${s}<input class="mark" type="number" min="0" max="100" value="${80+i}"></label>`));function generate(){const marks=[...document.querySelectorAll(".mark")].map(x=>Number(x.value));if(marks.some(x=>x<0||x>100)){alert("Marks must be 0-100");return}const total=marks.reduce((a,b)=>a+b,0);const per=total/marks.length;const grade=per>=75?"O":per>=60?"A":per>=45?"B":"F";document.getElementById("result").innerHTML=`<h2>${document.getElementById("name").value}</h2><p>Total: ${total}/${marks.length*100}</p><p>Percentage: ${per.toFixed(2)}%</p><p>Grade: ${grade}</p>`;}